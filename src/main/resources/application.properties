# Server configuration
server.port=8081

# Database configuration
spring.datasource.url=jdbc:mysql://localhost:3306/jobs
spring.datasource.username=abhinav
spring.datasource.password=@Abhinav321

# JPA and Hibernate configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JPA Database initialization
spring.jpa.defer-datasource-initialization=true
spring.sql.init.mode=always

# Hibernate naming strategy
#spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Logging configuration
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Security configuration
# If you need to verify JWT tokens or integrate with the Auth service, configure it here.
# Example:
# spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8080/auth/realms/jobportal
# spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:8080/auth/realms/jobportal/protocol/openid-connect/certs
